#!/usr/bin/env sh
# Maven wrapper script for ColdHarbor Control Plane

if command -v mvn >/dev/null 2>&1; then
    exec mvn "$@"
else
    echo "[ERROR] mvn command not found in PATH" >&2
    exit 1
fi
